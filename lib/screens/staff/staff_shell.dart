import 'staff_home_screen.dart';

// Single-screen shell for now — StaffHomeScreen already gates content by RoleId/permissions.
// A bottom-nav or drawer shell can replace this once more staff module screens exist.
typedef StaffShell = StaffHomeScreen;
